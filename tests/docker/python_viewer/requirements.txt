# aiortc 1.9.0 doesn't expose RTCBundlePolicy on Python 3.12 (works on
# 3.10/3.11). The CI container is espressif/idf:release-v5.5 which
# ships Python 3.12, so we need aiortc >= 1.10. The viewer also needs
# av >= 13 to compile against ffmpeg 8.x on macOS local runs (the
# CI image's older ffmpeg is fine with either, so the floor is set
# by the macOS dev path).
aiortc>=1.10
boto3>=1.34
botocore>=1.34
websockets>=12.0
av>=13.0
