## IDF Component Manager Manifest File
## Linux-target canary — depends only on the espressif-port components that
## already have IDF_TARGET=linux build paths today (esp_webrtc_utils:7-9 in
## components/esp_webrtc_utils/CMakeLists.txt). Adding kvs_webrtc / kvs_signaling /
## app_webrtc here requires those components to grow Linux-target conditionals
## first (TODO tracked separately).
dependencies:
  esp_webrtc_utils:
    path: ${KVS_SDK_PATH}/components/esp_webrtc_utils
    version: "*"
